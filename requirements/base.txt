# OS-agnostic dependencies. torch itself is installed separately per-OS
# (see mac.txt / windows.txt / linux.txt) because the correct build differs
# by platform and GPU availability.

flwr[simulation]
transformers
peft
opacus
datasets
evaluate
scikit-learn
pandas
numpy
pyyaml
tqdm
matplotlib
accelerate
seaborn
